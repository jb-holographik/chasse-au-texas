# URL publique du Worker Cloudflare
VITE_RESERVATION_API_URL=https://cat-reservation.holographik.workers.dev

# Secrets Worker (à configurer via `yarn worker:secret`, pas dans ce fichier)
# RESEND_API_KEY=re_xxxxxxxx
